<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/headers.ejs') -%>
    <!-- <link rel="stylesheet" href="/stylesheets/style.css"> -->
</head>

<body class="bg-dark">
    <form method="post" action="/api/signin"
        class="mx-auto col-sm-9 col-md-8 col-lg-6 p-5 bg-light text-dark rounded">
        <div class="form-group">
            <label>Username</label>
            <input name="userName" type="text" class="form-control">
            <small class="form-text text-danger">* required</small>
        </div>
        <div class="form-group">
            <label>Password</label>
            <input name="password" type="password" class="form-control">
            <small class="form-text text-danger">* required</small>
        </div>
        <button type="submit" class="btn btn-primary">SignUp</button>
        <div class="form-check mt-3 p-0">
            <div class="alert alert-<%= color%> text-justify" role="alert">
                <%= message %>
                <% if (message === "The password is incorrect.") { %>
                    <br><span>if you forgot your password, <a href="/api/newPass">click here</a></span>
                <% } %>
            </div>
        </div>
    </form>
</body>

</html>